# Change Site Colors

You can change the colors of a Docusaurus site in `src/css/custom.css`.

Generate a new color palette using the [Infima color generator](https://docusaurus.io/docs/styling-layout#styling-your-site-with-infima), then replace the existing values with the ones you generate.
